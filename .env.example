GitHub → Add file → Create new file
Name: `.env.example`
Isi:
```
OPENAI_API_KEY=sk-proj-your-key-here
```
Commit: "Add API key template"
```
FILE 2: railway.json

```
Add file → Create new file
Name: `railway.json`
Isi:
```
```
{
  "deploy": {
    "startCommand": "uvicorn ai_tutor:app --host 0.0.0.0 --port $PORT"
  }
}
```
```
Commit: "Add Railway deploy"
```
SETELAH 2 FILE:

```
Repo 100% → Railway 1-click → LIVE!
```
ACTION NOW:

```
Upload 2 file → Screenshot repo final → Gw kasih Railway steps!
```
